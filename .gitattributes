# 저장소 안에서는 LF로 통일하고, 체크아웃 시 OS에 맞게 변환한다.
* text=auto eol=lf

# Windows 전용 스크립트는 CRLF 유지
*.bat text eol=crlf
gradlew text eol=lf

# 바이너리 — 줄바꿈 변환 금지
*.jar binary
*.png binary
*.webp binary
*.keep binary
